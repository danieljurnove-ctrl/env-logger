name: tests

on:
  push:
  pull_request:

jobs:
  pytest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          # 3.11 is what Raspberry Pi OS Bookworm ships, and the Pi is where this
          # actually runs. Testing on something newer would let a 3.12-only
          # construct through.
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r pi/requirements.txt pytest

      - name: Run tests
        working-directory: pi
        # The whole directory, not one file by name: naming test_app.py meant
        # test_dashboard.py was silently never collected, and CI stayed green
        # while the test it was added to run did not exist as far as CI knew.
        run: python -m pytest -q

  shell:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check shell scripts
        run: |
          bash -n pi/backup.sh
          bash -n pi/install.sh
          shellcheck pi/backup.sh pi/install.sh
